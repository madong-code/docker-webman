# 注意：overlay/*.tar.xz 与 extension/pie.phar 是离线构建资源，\n# 已随仓库一起提供（约 7MB），不加入忽略列表 ——\n# 这样任何构建机（含国内 CI / 无外网服务器）都不需要访问 GitHub 即可构建。\n\n# 编辑器 / 系统\n.idea/\n.vscode/\n.DS_Store\nThumbs.db\n\n# 编辑器贴图可能落到项目根目录，不要提交\nimage*.png\n\n# 构建 / 调试日志\n*.log\n